{% extends 'base.html.twig' %}

{% block title %}{{ 'home.title'|trans }}{% endblock %}

{% block body %}
    <main>

        {# SECTION HERO #}
        <section class="hero-section position-relative text-light">
            <div class="p-5 text-center bg-image" style="
                {# Image d'un laboratoire #}
                background-image: url('{{ asset('images/home-hero.jpg') }}');
                height: 400px;
                background-size: cover;
                background-position: center;
                ">
                <div class="mask d-flex justify-content-center align-items-center h-100" style="background-color: rgba(0, 0, 0, 0.6)">
                    <div class="text-white">
                        <h1 class="mb-3 display-3 fw-bold" style="text-shadow:0 2px 6px rgba(0,0,0,0.5);">{{ 'home.hero.title'|trans }}</h1>
                        <h4 class="mb-4">{{ 'home.hero.subtitle'|trans }}</h4>
                        <a class="btn btn-primary btn-lg m-2" href="{{ path('app_about') }}" role="button">{{ 'home.hero.button_discover'|trans }}</a>
                        <a class="btn btn-outline-light btn-lg m-2" href="{{ path('app_resource_index') }}" role="button">{{ 'home.hero.button_resources'|trans }}</a>
                    </div>
                </div>
            </div>
        </section>

        {# SECTION AVANTAGES #}
        <section class="py-5 bg-light">
            <div class="container">
                <h2 class="text-center fw-bold mb-5">{{ 'home.features.title'|trans }}</h2>

                <div class="row g-4">
                    <div class="col-md-4">
                        <div class="card shadow-sm h-100 border-0">
                            <div class="card-body p-4 text-center">
                                <div class="display-5 mb-3">🧪</div>
                                <h5 class="fw-bold mb-3">{{ 'home.features.feature1.title'|trans }}</h5>
                                <p>{{ 'home.features.feature1.text'|trans }}</p>
                            </div>
                        </div>
                    </div>

                    <div class="col-md-4">
                        <div class="card shadow-sm h-100 border-0">
                            <div class="card-body p-4 text-center">
                                <div class="display-5 mb-3">📅</div>
                                <h5 class="fw-bold mb-3">{{ 'home.features.feature2.title'|trans }}</h5>
                                <p>{{ 'home.features.feature2.text'|trans }}</p>
                            </div>
                        </div>
                    </div>

                    <div class="col-md-4">
                        <div class="card shadow-sm h-100 border-0">
                            <div class="card-body p-4 text-center">
                                <div class="display-5 mb-3">🔒</div>
                                <h5 class="fw-bold mb-3">{{ 'home.features.feature3.title'|trans }}</h5>
                                <p>{{ 'home.features.feature3.text'|trans }}</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        {# SECTION CTA #}
        <section class="py-5 bg-primary text-light text-center">
            <div class="container">
                <h2>{{ 'home.cta.title'|trans }}</h2>
                <p class="lead">{{ 'home.cta.subtitle'|trans }}</p>
                <a class="btn btn-light btn-lg" href="{{ path('app_register') }}" role="button">{{ 'home.cta.button'|trans }}</a>
            </div>
        </section>
    </main>
{% endblock %}
