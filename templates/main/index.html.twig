{% extends 'base.html.twig' %}

{% block title %}{{ 'app.index.title'|trans }}{% endblock %}

{% block body %}
    <main>

        {# SECTION HERO #}
        <section class="hero-section position-relative text-light">
            <div class="p-5 text-center bg-image" style="
                {# Image d'un laboratoire #}
                background-image: url('{{ asset('images/home-hero.jpg') }}');
                height: 400px;
                background-size: cover;
                background-position: center;
                ">
                <div class="mask d-flex justify-content-center align-items-center h-100" style="background-color: rgba(0, 0, 0, 0.6)">
                    <div class="text-white">
                        <h1 class="mb-3 display-3 fw-bold" style="text-shadow:0 2px 6px rgba(0,0,0,0.5);">{{ 'app.index.h1'|trans }}</h1>
                        <h4 class="mb-4">{{ 'app.index.h4'|trans }}</h4>
                        <a class="btn btn-primary btn-lg m-2" href="{{ path('app_about') }}" role="button">{{ 'app.index.discover'|trans }}</a>
                        <a class="btn btn-outline-light btn-lg m-2" href="{{ path('app_resource_index') }}" role="button">{{ 'app.index.showRessource'|trans }}</a>
                    </div>
                </div>
            </div>
        </section>

        {# SECTION AVANTAGES #}
        <section class="py-5 bg-light">
            <div class="container">
                <h2 class="text-center fw-bold mb-5">{{ 'app.index.h2'|trans }}</h2>

                <div class="row g-4">
                    <div class="col-md-4">
                        <div class="card shadow-sm h-100 border-0">
                            <div class="card-body p-4 text-center">
                                <div class="display-5 mb-3">🧪</div>
                                <h5 class="fw-bold mb-3">{{ 'app.index.avantages.access.h5'|trans }}</h5>
                                <p>{{ 'app.index.avantages.access.p'|trans }}</p>
                            </div>
                        </div>
                    </div>

                    <div class="col-md-4">
                        <div class="card shadow-sm h-100 border-0">
                            <div class="card-body p-4 text-center">
                                <div class="display-5 mb-3">📅</div>
                                <h5 class="fw-bold mb-3">{{ 'app.index.avantages.gestion.h5'|trans }}</h5>
                                <p>{{ 'app.index.avantages.gestion.p'|trans }}</p>
                            </div>
                        </div>
                    </div>

                    <div class="col-md-4">
                        <div class="card shadow-sm h-100 border-0">
                            <div class="card-body p-4 text-center">
                                <div class="display-5 mb-3">🔒</div>
                                <h5 class="fw-bold mb-3">{{ 'app.index.avantages.secure.h5'|trans }}</h5>
                                <p>{{ 'app.index.avantages.secure.p'|trans }}</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        {# SECTION CTA #}
        <section class="py-5 bg-primary text-light text-center">
            <div class="container">
                <h2>{{ 'app.index.cta.h2'|trans }}</h2>
                <p class="lead">{{ 'app.index.cta.p'|trans }}</p>
                <a class="btn btn-light btn-lg" href="{{ path('app_register') }}" role="button">{{ 'app.index.cta.link'|trans }}</a>
            </div>
        </section>
    </main>
{% endblock %}
